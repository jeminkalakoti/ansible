- name: Variable from args
  hosts: local
  connection: local 
  tasks:
  - name: print the course info
    ansible.builtin.debug:
      msg: "Course Name is: {{ COURSE }}, Duration is: {{ DURATION }}, Trainer name is {{ Traine }}"